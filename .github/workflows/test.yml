
name: Build dispatcherd

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6
      
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'

      - name: Install libpcap # required by naabu runner
        run: sudo apt install libpcap-dev

      - name: Install Task
        uses: go-task/setup-task@v1

      - name: Install mockery
        run: go install github.com/vektra/mockery/v3@latest

      - name: Generate mocks
        run: task genmock

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9

  test:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v6

    - name: Install Task
      uses: go-task/setup-task@v1

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.25'

    - name: Install libpcap # required by naabu runner
      run: sudo apt install libpcap-dev

    - name: Install mockery
      run: go install github.com/vektra/mockery/v3@latest

    - name: Build
      run: task build

    - name: Test
      run: task test
